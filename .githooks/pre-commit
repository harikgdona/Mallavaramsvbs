#!/bin/sh
# Enforce: no node_modules/.next/out in commits; no single file > 50 MB.
REPO_ROOT=$(git rev-parse --show-toplevel)
if command -v node >/dev/null 2>&1; then
  node "$REPO_ROOT/scripts/verify-staged.mjs" || exit 1
fi
exit 0
