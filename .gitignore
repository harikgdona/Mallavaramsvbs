# ——— Dependencies & package managers (never commit) ———
node_modules/
.pnp
.pnp.js
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
package-lock.json.bak

# ——— Next.js / build output (never commit) ———
.next/
out/
dist/
build/
.turbo/
.vercel/

# ——— Env & secrets ———
.env
.env.local
.env*.local
.mallavaram-admin-password
export-login-debug.png
.env.development.local
.env.test.local
.env.production.local

# ——— Logs & debug ———
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ——— Test / coverage ———
coverage/
.nyc_output/

# ——— TypeScript / tooling cache ———
*.tsbuildinfo
next-env.d.ts.bak

# ——— Configure exports for npm run publish-site (may contain huge data URLs) ———
site-manual-from-browser.json
site-bundle-from-browser.json

# ——— Local / backup component copies (not for main branch) ———
components/TopHeader.before-reset.tsx
components/topHeader_26032001.tsx

# ——— IDE / OS ———
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
*.swo

# ——— Large local assets (optional: uncomment if you keep huge files here) ———
# public/uploads/
